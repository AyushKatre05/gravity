[build]
target = "index.html"
dist   = "dist"
public_url = "/"

[watch]
ignore = []

[serve]
port    = 3000
open    = false
proxy_backend = "http://localhost:8080"

[[proxy]]
rewrite = "/api"
backend = "http://localhost:8080/api"
